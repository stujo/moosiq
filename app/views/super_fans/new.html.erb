<div class="page-header">
  <%= link_to artist_super_fans_path(@artist), :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New Super Fan for <%= @artist.name %></h1>
</div>

<%= render 'form' %>
